<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Upload Logo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
</head>
<body>
    <h1>Upload Logo</h1>

    <!-- show current logo from static/images/logo.png -->
    <div>
        <p>Current logo:</p>
        <img src="{{ url_for('static', filename='images/logo.png') }}" alt="Logo" style="max-width:240px; height:auto;">
    </div>

    <!-- form to submit a new logo file -->
    <form action="{{ url_for('upload_logo') }}" method="post" enctype="multipart/form-data">
        <label>
            Choose image:
            <input type="file" name="logo" accept="image/*" required>
        </label>
        <button type="submit">Upload</button>
    </form>

    {% if message %}
        <p>{{ message }}</p>
    {% endif %}
</body>
</html>